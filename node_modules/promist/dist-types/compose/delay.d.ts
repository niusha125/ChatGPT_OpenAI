export default function delay(ms: number, delayRejection?: boolean): {
    <A, T>(promise: A & Promise<T>, create?: false | undefined): A & Promise<T>;
    <T>(promise: Promise<T>, create?: boolean | undefined): Promise<T>;
};
