export declare type TExecutor = (resolve: (val?: any) => void, reject: (reason: Error) => void) => void;
export default lazy;
declare function lazy(executor: TExecutor): Promise<any>;
declare namespace lazy {
    var fn: <T>(fn: () => T | Promise<T>) => Promise<T>;
}
