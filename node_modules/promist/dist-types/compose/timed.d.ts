import { ITimed } from "../types";
export default timed;
declare function timed<A, T>(promise: A & Promise<T>, create?: false): A & ITimed & Promise<T>;
declare function timed<T>(promise: Promise<T>, create?: boolean): ITimed & Promise<T>;
