import { ICancellable } from "../types";
export default cancellable;
declare function cancellable<A, T>(promise: A & Promise<T>, create?: false): A & ICancellable & Promise<T>;
declare function cancellable<T>(promise: Promise<T>, create?: boolean): ICancellable & Promise<T>;
