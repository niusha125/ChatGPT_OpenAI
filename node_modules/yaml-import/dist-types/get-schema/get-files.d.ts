import { IOptions, IFileDefinition } from "../types";
export default function getFiles(paths: string[], cwd: string, options: IOptions, recursive?: boolean): IFileDefinition[];
export declare function getFromDir(absolute: string, options: IOptions, recursive?: boolean): string[];
