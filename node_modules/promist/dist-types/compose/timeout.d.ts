import { ICancellable, IDeferrable } from "../types";
export default function timeout(ms: number, reason?: boolean | Error): {
    <A, T>(promise: A & Promise<T>, create?: false | undefined): A & ICancellable & IDeferrable & Promise<T>;
    <T>(promise: Promise<T>, create?: boolean | undefined): ICancellable & IDeferrable & Promise<T>;
};
