import { INormalizeOptions } from './types';
/**
 * Normalizes an Error -it assumes an instance of Error is passed. It guarantees the error will have a `name`, `message`, and `stack` properties.
 */
export default function normalize<T extends Error>(error: T, options?: INormalizeOptions): T & {
    stack: string;
};
