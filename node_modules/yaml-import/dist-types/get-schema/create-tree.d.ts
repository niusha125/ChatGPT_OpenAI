import yaml from 'js-yaml';
import { IOptions } from "../types";
export default function createTree(file: string, directory: string, options: IOptions, schemas: yaml.Schema[], recursive?: boolean): any;
