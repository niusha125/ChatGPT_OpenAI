import { IStatus } from "../types";
export default status;
declare function status<A, T>(promise: A & Promise<T>, create?: false): A & IStatus & Promise<T>;
declare function status<T>(promise: Promise<T>, create?: boolean): IStatus & Promise<T>;
